#include "cola.h"
#define MAX_SIZE 30

typedef struct{

    char nome[MAX_SIZE];
    char marca[MAX_SIZE];
    char modelo[MAX_SIZE];
    char ubicacion[MAX_SIZE];
    TCOLA cola_impresion;
    
}impresora;